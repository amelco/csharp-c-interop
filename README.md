Basic example of calling C functions within C# code.

## Usage
Run `build.sh`. 
It will compile the C code in a shared (dynamic) library (`.so` dor linux and `.dll` for windows),
compile the C# code and execute it.
